apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: minecraft-servers
spec:
  generators:
    - git:
        repoURL: https://github.com/FullMetal667/minecraft-gitops.git
        revision: main
        directories:
          - path: clusters/overlays/*
  template:
    metadata:
      name: '{{path.basename}}'
    spec:
      project: minecraft
      source:
        repoURL: https://github.com/deinuser/minecraft-gitops.git
        targetRevision: main
        path: '{{path}}'
      destination:
        server: https://kubernetes.default.svc
        namespace: default
      syncPolicy:
        automated:
          prune: true
          selfHeal: true

