apiVersion: apps/v1
kind: Deployment
metadata:
  name: minecraft-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: minecraft
  template:
    metadata:
      labels:
        app: minecraft
    spec:
      securityContext:
        fsGroup: 1001270000
      serviceAccountName: minecraft-sa
      volumes:
        - name: world-data
          persistentVolumeClaim:
            claimName: world-data
        - name: backup
          persistentVolumeClaim:
            claimName: backup-pvc
        - name: overrides
          persistentVolumeClaim:
            claimName: overrides-storage
        - name: server-properties
          configMap:
            name: mc-server-properties
            defaultMode: 0755
        - name: startserver
          configMap:
            name: startserver
            defaultMode: 0755
        - name: user-jvm-args
          configMap:
            name: user-jvm-args
            defaultMode: 0755
        - name: mcrcon-commands
          configMap:
            name: minecraft-commands
            defaultMode: 0755
        - name: eula
          configMap:
            name: mc-eula
            defaultMode: 0755
      containers:
        - name: minecraft
          image: "docker.io/chrisroth667/minecraft:0.0.0"
          imagePullPolicy: Always
          ports:
            - containerPort: 25565
              protocol: TCP
          env:
            - name: EULA
              value: "true"
          envFrom:
            - secretRef:
                name: mc-env
            - secretRef:
                name: secret-curseforge
            - secretRef:
                name: mc-env-ghk75bcck5 
          command: ["/bin/bash","-lc"]
          args: ["/data/startserver.sh"]
          volumeMounts:
            - name: mcrcon-commands
              mountPath: /data/commands.txt
              subPath: commands.txt
            - name: world-data
              mountPath: /srv/mc/world
            - name: overrides
              mountPath: /data/overrides
            - name: backup
              mountPath: /data/simplebackups
            - name: startserver
              mountPath: /data/startserver.sh
              subPath: startserver.sh
            - name: server-properties
              mountPath: /srv/mc/server.properties
              subPath: server.properties
            - name: user-jvm-args
              mountPath: /srv/mc/user_jvm_args.txt
              subPath: user_jvm_args.txt
            - name: eula
              mountPath: /data/eula.txt
              subPath: eula.txt
          terminationMessagePath: /dev/termination-log
          terminationMessagePolicy: File
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
      dnsPolicy: ClusterFirst
      schedulerName: default-scheduler
