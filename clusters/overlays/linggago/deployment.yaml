apiVersion: apps/v1
kind: Deployment
metadata:
  name: minecraft-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: minecraft
  template:
    metadata:
      labels:
        app: minecraft
    spec:
      securityContext: {}
      serviceAccountName: minecraft-sa
      volumes:
        - name: world-data
          persistentVolumeClaim:
            claimName: world-data
        - name: backup
          persistentVolumeClaim:
            claimName: backup-pvc
        - name: overrides
          persistentVolumeClaim:
            claimName: overrides-storage
        - name: server-properties
          configMap:
            name: mc-server-properties
            defaultMode: 0755
        - name: user-jvm-args
          configMap:
            name: user-jvm-args
            defaultMode: 0755
        - name: mcrcon-commands
          configMap:
            name: minecraft-commands
            defaultMode: 0755
      containers:
        - name: minecraft
          image: "docker.io/chrisroth667/minecraft:0.0.0"
          imagePullPolicy: Always
          ports:
            - containerPort: 25565
              protocol: TCP
          env:
            - name: EULA
              value: "true"
          envFrom:
            - secretRef:
                name: mc-env
          args:
            - |
              set -e
              cd /srv/mc
              exec java ${JVM_ARGS:-} -jar forge-1.20.1-47.2.0.jar nogui
          volumeMounts:
            - name: mcrcon-commands
              mountPath: /data/commands.txt
              subPath: commands.txt
            - name: world-data
              mountPath: /data/world
            - name: overrides
              mountPath: /data/overrides
            - name: backup
              mountPath: /data/simplebackups
            - name: server-properties
              mountPath: /data/server.properties
              subPath: server.properties
            - name: user-jvm-args
              mountPath: /data/user-jvm-args.txt
              subPath: user-jvm-args.txt
          terminationMessagePath: /dev/termination-log
          terminationMessagePolicy: File
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
      dnsPolicy: ClusterFirst
      schedulerName: default-scheduler
